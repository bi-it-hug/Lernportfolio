version: "3.8"

services:
    nginx:
        image: nginx:latest
        container_name: nginx_server
        ports:
            - "5743:80"
        volumes:
            - ./www:/usr/share/nginx/html:ro
        restart: unless-stopped
