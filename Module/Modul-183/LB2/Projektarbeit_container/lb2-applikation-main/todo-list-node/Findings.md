# Eigene Findings (Einzelarbeit)

Stand: 2026-05-29  
Scope: `todo-list-node` (vollständiger Code-Review aller App-Dateien)

> **Remediation (2026-05-29):** Die unten dokumentierten Schwachstellen wurden in der Codebasis behoben (Session-Auth, Prepared Statements, bcrypt, CSRF, Helmet, Rate-Limiting, Output-Encoding, IDOR-Fixes, SSRF-Entfernung, Admin-Check, u.a.).

| Datei                          | Zeile(n)                | Schwachstelle                              | Risiko | Kurzbeschreibung                                                                                                                     |
| ------------------------------ | ----------------------- | ------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| `app.js`                       | 19–23                   | Unsichere Session-Konfiguration            | Hoch   | `secret` ist hartcodiert (`secret`), `saveUninitialized: true`, keine Cookie-Flags wie `httpOnly`, `secure`, `sameSite`, keiTimeout. |
| `app.js`                       | 134–138                 | Authentisierung nur ueber Client-Cookies   | Hoch   | `activeUserSession()` prueft nur `req.cookies.username`; Cookie-Manipulation erlaubt Session-Bypass.                                 |
| `app.js`                       | 51–58                   | Fehlende Rollenpruefung Admin              | Hoch   | `/admin/users` ist nur durch "eingeloggt" geschuetzt, nicht durch Admin-Rolle.                                                       |
| `app.js`                       | 112–115, 118–121        | Fehlende Session-Pruefung Search-Endpunkte | Mittel | `/search` und `/search/v2/` sind ohne Session-Pruefung erreichbar.                                                                   |
| `config.js`                    | 2–5                     | Klartext-Zugangsdaten im Code              | Hoch   | DB-Host/User/Passwort sind hartcodiert und koennen geleakt werden.                                                                   |
| `index.js`                     | 6                       | Reflected XSS ueber Benutzername           | Mittel | `req.cookies.username` wird ohne Escaping direkt in HTML ausgegeben.                                                                 |
| `login.js`                     | 7–9, 74                 | Credentials ueber GET                      | Hoch   | Benutzername/Passwort werden in URL uebertragen (`req.query`, `method="get"`).                                                       |
| `login.js`                     | 37                      | SQL Injection                              | Hoch   | Query baut SQL per String-Konkatenation mit `username`.                                                                              |
| `login.js`                     | 48                      | Passwort im Klartext verarbeitet           | Hoch   | Passwortvergleich erfolgt direkt gegen DB-Klartext (`password == db_password`).                                                      |
| `login.js`                     | 54, 58                  | User Enumeration                           | Mittel | Unterschiedliche Fehlermeldungen (`Username does not exist` vs `Incorrect password`).                                                |
| `login.js`                     | 26–27                   | Unsichere Cookie-Session                   | Hoch   | Login setzt nur normale Cookies (`username`, `userid`) statt serverseitiger Session mit sicheren Flags.                              |
| `edit.js`                      | 16                      | SQL Injection                              | Hoch   | `id` aus Query wird direkt in SQL eingebaut.                                                                                         |
| `edit.js`                      | 16                      | IDOR (fremde Tasks lesbar)                 | Hoch   | Task wird nur per `ID` geladen, ohne Pruefung auf Eigentuemer (`userID`).                                                            |
| `edit.js`                      | 32                      | Reflected XSS im Formular                  | Mittel | `title` wird unescaped in `value="..."` ausgegeben.                                                                                  |
| `savetask.js`                  | 10, 22, 24              | SQL Injection (mehrfach)                   | Hoch   | `id`, `title`, `state`, `userid` werden in `SELECT/INSERT/UPDATE` unescaped verwendet.                                               |
| `savetask.js`                  | 24                      | IDOR (fremde Tasks veraenderbar)           | Hoch   | `UPDATE ... WHERE ID = ...` ohne `AND userID = ...`.                                                                                 |
| `savetask.js`                  | 16–25                   | Fehlender CSRF-Schutz                      | Mittel | Schreibender POST-Endpunkt ohne CSRF-Token/Origin-Pruefung.                                                                          |
| `search.js`                    | 9–15                    | SSRF / unkontrollierter Provider           | Hoch   | `provider` aus Request wird in Ziel-URL uebernommen.                                                                                 |
| `search.js`                    | 11, 15                  | Zugriff auf fremde Suchdaten               | Hoch   | `userid` kommt aus Client-POST und kann manipuliert werden.                                                                          |
| `search/v2/index.js`           | 12                      | SQL Injection                              | Hoch   | `userid` und `terms` werden direkt in SQL eingebaut.                                                                                 |
| `search/v2/index.js`           | 3–12                    | Fehlende Auth + IDOR                       | Hoch   | Endpoint kann direkt aufgerufen werden und erlaubt Abfrage fremder User-Tasks ueber `userid`.                                        |
| `search/v2/index.js`           | 15                      | Stored/Reflected XSS bei Ausgabe           | Mittel | `row.title` wird ohne Encoding in HTML (`<br />`) ausgegeben.                                                                        |
| `admin/users.js`               | 19                      | Preisgabe sensibler Daten                  | Hoch   | Passwoerter werden im HTML als `hidden` Feld an den Client gesendet.                                                                 |
| `fw/header.js`                 | 23                      | SQL Injection ueber Cookie                 | Hoch   | `userid` aus Cookie wird direkt in SQL eingebaut.                                                                                    |
| `fw/db.js`                     | 15–18                   | Unsicheres Query-Muster                    | Mittel | `executeStatement(statement)` fuehrt rohe SQL-Strings aus; zentraler Enabler fuer SQLi.                                              |
| `user/tasklist.js`             | 17                      | SQL Injection ueber Cookie                 | Hoch   | `req.cookies.userid` fliesst ungefiltert in SQL ein.                                                                                 |
| `user/tasklist.js`             | 23                      | Stored XSS in Taskliste                    | Mittel | `row.title` wird direkt in HTML gerendert.                                                                                           |
| `user/tasklist.js`             | 26                      | Unsicheres Delete per GET                  | Mittel | Delete-Link nutzt GET (`delete?id=...`) und ist CSRF-anfaellig.                                                                      |
| `user/backgroundsearch.js`     | 6, 32–37                | Manipulierbarer Suchprovider im Client     | Mittel | Hidden-Feld `searchurl` kann clientseitig geaendert werden und steuert Backend-Request.                                              |
| `package.json`                 | 12                      | Veraltete/risikobehaftete Abhaengigkeiten  | Mittel | U. a. `express 4.17.1` ist alt; Update auf aktuelle Patch-Versionen noetig.                                                          |
| `fw/footer.js`, `nodemon.json` | —                       | Kein direktes Security-Finding             | Tief   | In diesen Dateien wurde kein sicherheitsrelevanter Fehler festgestellt.                                                              |
| `app.js`                       | 134–138                 | Cookie `username`/`userid` nicht gekoppelt | Hoch   | `activeUserSession()` prüft nur `username`; `userid` kann unabhängig manipuliert werden → Zugriff auf fremde Tasks/Rollen.           |
| `app.js`                       | 19–23, `login.js` 26–27 | Zwei parallele Auth-Mechanismen            | Mittel | `express-session` ist konfiguriert, Login nutzt aber separate Cookies; Session und Cookies sind nicht synchron.                      |
| `app.js`                       | 93–98                   | Inkonsistente Auth auf `/profile`          | Mittel | Route prüft `req.session.loggedin`, Login setzt aber nur Cookies – Profil ist praktisch ungeschützt bzw. nutzt anderen Auth-Weg.     |
| `app.js`                       | 93–95                   | Reflected XSS auf `/profile` (potenziell)  | Mittel | `req.session.username` wird unescaped in HTML eingebettet; bei Session-Manipulation (schwaches Secret) ausnutzbar.                   |
| `app.js`                       | 85–89                   | Unsicheres Logout                          | Mittel | Cookies werden auf `''` gesetzt statt gelöscht; keine `httpOnly`/`secure`/`sameSite`-Flags beim Setzen/Löschen.                      |
| `app.js`                       | —                       | Fehlende Security-Header                   | Mittel | Kein Helmet o.ä.; es fehlen CSP, `X-Frame-Options`, `X-Content-Type-Options` → Clickjacking/MIME-Sniffing möglich.                   |
| `app.js`                       | 71–82, 125              | Kein Brute-Force-/Rate-Limiting            | Mittel | Login und Search-Endpunkte können ohne Begrenzung massenhaft aufgerufen werden (Passwort-Raten, DoS).                                |
| `app.js`                       | 41–48                   | POST `/` ohne CSRF-Schutz                  | Mittel | Authentifizierte POST-Anfragen auf die Startseite ohne Token-Prüfung.                                                                |
| `app.js`                       | 37, 46, 56, 66          | Relative Redirects                         | Tief   | `res.redirect('login')` statt `/login` – in bestimmten Kontexten Open-Redirect-Risiko.                                               |
| `login.js`                     | 81                      | Passwortfeld als Klartext (`type="text"`)  | Mittel | Passwort ist auf dem Bildschirm sichtbar (Shoulder Surfing, Browser-History in URL bei GET).                                         |
| `login.js`                     | 61–64                   | Sensible Daten in Server-Logs              | Mittel | `console.log(results)` schreibt DB-Zeilen inkl. Passwort-Hash/Klartext in Logs; Fehler werden nur geloggt.                           |
| `login.js`                     | 48                      | Unsicherer Passwortvergleich (`==`)        | Tief   | Nicht constant-time; theoretisch Timing-Angriffe auf das Passwort möglich.                                                           |
| `fw/header.js`                 | 12–13                   | Externe Skripte ohne SRI                   | Mittel | jQuery/jQuery-Validate von CDN ohne Subresource-Integrity → Supply-Chain-/MITM-Risiko.                                               |
| `fw/header.js`                 | 35                      | Admin-Rolle per Magic Number               | Tief   | `roleid === 1` ist hartcodiert; schwache Rollenmodellierung, leicht zu erraten.                                                      |
| `fw/header.js`                 | 24                      | Information Disclosure in Logs             | Tief   | Rollenabfrage (`stmt`) wird per `console.log` ausgegeben.                                                                            |
| `fw/db.js`                     | 6–17                    | DB-Verbindungen nicht geschlossen          | Mittel | Jeder Query öffnet neue Connection ohne `close()` → Ressourcenerschöpfung / DoS bei Last.                                            |
| `config.js`                    | 4                       | DB-User mit Root-Rechten                   | Hoch   | Applikation verbindet sich als `root` – bei Kompromittierung volle DB-Rechte statt least privilege.                                  |
| `admin/users.js`               | 19                      | Reflected/Stored XSS (Username/Rolle)      | Mittel | `record.username` und `record.title` werden unescaped in HTML-Tabelle gerendert.                                                     |
| `admin/users.js`               | 28                      | Userliste beim Start gecacht               | Mittel | `module.exports = { html: getHtml() }` lädt Passwörter einmalig in den Speicher; keine Aktualisierung, Daten bleiben im RAM.         |
| `search.js`                    | 15                      | SSRF-URL-Parsing-Bypass                    | Hoch   | `http://localhost:3000` + `provider` erlaubt z.B. `@169.254.169.254/` → Request an andere Hosts (Cloud-Metadata).                    |
| `search.js`                    | 15                      | Fehlende URL-Kodierung                     | Mittel | `userid`/`terms` werden unkodiert an `/search/v2/` angehängt → Parameter-Injection in Folge-Request.                                 |
| `search.js`                    | 13                      | DoS durch künstliche Verzögerung           | Mittel | `sleep(1000)` pro Search-Request ohne Rate-Limit → Ressourcenbindung bei vielen parallelen Anfragen.                                 |
| `search/v2/index.js`           | 15                      | DOM-basierte XSS-Kette (Client)            | Mittel | Antwort landet in `backgroundsearch.js` via `$("#result").html(data)` – HTML/JS aus Suchresultaten wird ausgeführt.                  |
| `user/backgroundsearch.js`     | 34                      | XSS über `userid`-Cookie im Script         | Hoch   | `userid` wird ungeprüft in JavaScript eingebettet (`userid = `+cookie+`;`) → Script-Injection möglich.                               |
| `user/backgroundsearch.js`     | 39                      | Unsichere DOM-Manipulation                 | Mittel | Suchergebnisse werden mit `.html()` statt `.text()` eingefügt → XSS wenn Backend HTML liefert.                                       |
| `user/tasklist.js`             | 26                      | Delete-Link ohne Backend-Route             | Mittel | Link `delete?id=...` existiert, aber kein Handler in `app.js` – deaktiviert oder später unsicher nachgerüstet.                       |
| `user/tasklist.js`             | 18                      | Taskdaten in Logs                          | Tief   | `console.log(result)` gibt Task-Inhalte in Server-Logs aus.                                                                          |
| `edit.js`                      | 29                      | Reflected XSS im Hidden-Feld `id`          | Mittel | `taskId` aus Query wird unescaped in `value="..."` geschrieben.                                                                      |
| `edit.js`                      | 11–13                   | Request-Daten in Logs                      | Tief   | `req.query` wird vollständig geloggt (kann sensible Parameter enthalten).                                                            |
| `savetask.js`                  | 17–18                   | Keine Eingabevalidierung `state`           | Mittel | `state` wird nicht gegen Whitelist geprüft und fliesst in SQL – zusaetzlicher Injection-Vektor.                                      |
| `savetask.js`                  | 21–24                   | Fehlendes `await` bei DB-Schreibzugriff    | Mittel | `executeStatement` ohne `await` → Fehler werden nicht behandelt, Integritaet der Daten unsicher.                                     |
| `savetask.js`                  | 27                      | Fehlende Re-Auth nach Änderung             | Tief   | Nach Speichern nur Statusmeldung, kein Redirect – erleichtert Replay/CSRF-Kombinationen.                                             |
| `package.json`                 | 7                       | Nodemon im Produktions-Startscript         | Tief   | `"start": "nodemon ..."` ist ein Dev-Tool; erhoeht Angriffsflaeche in produktiven Deployments.                                       |
| `app.js`                       | 136–137                 | Cookie-Leak in Logs                        | Tief   | `activeUserSession` loggt komplette `req.cookies` bei jedem Auth-Check.                                                              |
