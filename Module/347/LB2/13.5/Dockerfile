FROM eclipse-temurin:17-jdk-alpine

WORKDIR /app

COPY target/*.jar app.jar

EXPOSE 8080

ENV REFCARD_THEME=light
ENV REFCARD_RECAPTCHA_KEY_SITE=
ENV REFCARD_RECAPTCHA_KEY_SECRET=
ENV REFCARD_DB_URL=
ENV REFCARD_DB_USER=
ENV REFCARD_DB_PASSWORD=

ENTRYPOINT ["java", "-jar", "app.jar"]
